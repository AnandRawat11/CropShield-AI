# ─── Python Virtual Environment ──────────────────────────────────────────────
venv/
ai-api/venv/
__pycache__/
ai-api/**/__pycache__/
*.pyc
*.pyo

# ─── Datasets (large — never push to GitHub) ─────────────────────────────────
ai-api/dataset/
ai-api/dataset_merged/

# ─── Model Plots (generated artifacts, not needed in repo) ───────────────────
ai-api/model/training_curves.png
ai-api/model/confusion_matrix.png
ai-api/model/per_class_f1.png
# Model files (.h5) are committed — 45MB, safely under GitHub's 100MB limit
# class_indices.json and CSVs are also committed (small and useful)

# ─── Node.js ─────────────────────────────────────────────────────────────────
node_modules/
client/node_modules/
server/node_modules/
npm-debug.log*
yarn-debug.log*

# ─── Environment Variables (NEVER push secrets) ───────────────────────────────
.env
.env.local
# .env.production is committed intentionally — contains only public API URLs
# Vercel needs it during build to inject VITE_ variables
server/.env
ai-api/.env

# ─── Build Output ─────────────────────────────────────────────────────────────
client/dist/
client/build/

# ─── macOS System Files ───────────────────────────────────────────────────────
.DS_Store
**/.DS_Store

# ─── Logs ─────────────────────────────────────────────────────────────────────
*.log
logs/

# ─── IDE Files ────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ─── Misc ─────────────────────────────────────────────────────────────────────
futurePlans.txt